name: Mock Server and Azure Function Workflow

on:
  workflow_call:
    inputs:
      SERVER_URL:
        required: true
        type: string
      AZURE_FUNCTION_URL:
        required: true
        type: string

jobs:
  steps:
    runs-on: ubuntu-latest

    steps:
      - name: Define environment variables
        run: |
          echo "Defining environment variables..."
          echo "SERVER_URL=${{ inputs.SERVER_URL }}" >> $GITHUB_ENV
          echo "AZURE_FUNCTION_URL=${{ inputs.AZURE_FUNCTION_URL }}" >> $GITHUB_ENV

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: mock-server/package-lock.json

      - name: Build and start mock server
        run: |
          echo "Starting mock server..."
          cd mock-server
          npm ci
          npm run build
          nohup npm run start & # Run the mock server in the background
          cd ..

      - name: Wait for mock server to be ready
        run: |
          echo "Waiting for mock server to be ready..."
          timeout=120
          while [ $timeout -gt 0 ]; do
            if curl -f $SERVER_URL/todos; then
              echo "Mock server is ready!"
              break
            fi
            echo "Waiting for mock server... ($timeout seconds remaining)"
            sleep 5
            timeout=$((timeout-5))
          done

          if [ $timeout -le 0 ]; then
            echo "Mock server failed to start within timeout"
            exit 1
          fi

      - name: Test mock server endpoints
        run: |
          echo "Testing mock server endpoints..."
          curl -f $SERVER_URL/todos
          echo "Mock server tests passed!"

      - name: Install Azure Functions Core Tools
        run: |
          echo "Installing Azure Functions Core Tools..."
          npm install -g azure-functions-core-tools@4 --unsafe-perm

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Build and start Azure Function
        run: |
          echo "Starting Azure Function..."
          cd SimpleAzureFunction
          dotnet build
          API_URL=$SERVER_URL/todos func start --dotnet-isolated &
          cd ..

      - name: Wait for Azure Function to be ready
        run: |
          echo "Waiting for Azure Function to be ready..."
          timeout=120
          while [ $timeout -gt 0 ]; do
            if curl -f $AZURE_FUNCTION_URL/api/GetPublicApiData; then
              echo "Azure Function is ready!"
              break
            fi
            echo "Waiting for Azure Function... ($timeout seconds remaining)"
            sleep 5
            timeout=$((timeout-5))
          done

          if [ $timeout -le 0 ]; then
            echo "Azure Function failed to start within timeout"
            exit 1
          fi

      - name: Test Azure Function endpoints
        run: |
          echo "Testing Azure Function endpoints..."
          curl -f $AZURE_FUNCTION_URL/api/GetPublicApiData
          echo "Azure Function tests passed!"
